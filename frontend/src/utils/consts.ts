export const TOKEN_KEY = 'jwt';
export const MARGIN_TOP_CONTENT = '15rem';
export const FONT_FAMILY = 'Gill Sans';
export const BUTTON_RADIUS = '0.5rem';

export enum StatusCode {
  OK = 200,
  Created = 201,
}

export enum SeverityOption {
  Error = 'error',
  Success = 'success',
}

export enum Role {
  Admin = 'admin',
  User = 'user',
}
